package configuracion;

import java.util.prefs.Preferences;

public class Configuracion {
    //public static final Preferences prefs = Preferences.userRoot().node(Configuracion.class.getClass().getName());
    
    public static boolean conexionRealizada;
}
